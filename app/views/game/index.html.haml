.row
  /#map-canvas
.row
  .container
    #{@user.username} 
    %br
    %hr
    Player Level: 
    %span{ :class => 'playerlevel' }  
      #{@level}
      #{@passed_exp}

    .experience
      Experience: 
      %span{ :class => 'current-experience' } 
        = @current_experience
      \/
      %span{ :class => 'next-level-exp' }
        = @next_level_experience
    .level-from-db
      = @percent_to_level

    / Start exp progress bar
    .progress
      .progress-bar.progress-bar-warning{:role => "progressbar", :valuemin => "0", :valuemax => "1000", :style => "width: #{@percent_to_level}%;", :title => "Fuck You"} 

    .btn-group.hidden-xs.visible-sm.visible-md.visible-lg
      = render 'shared/add_experience_buttons', :add_class => ""
    .hidden-sm.hidden-md.hidden-lg.visible-xs
      = render 'shared/add_experience_buttons', :add_class => "btn-block"